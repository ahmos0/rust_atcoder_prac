/*use proconio::input;

fn main() {
    input! {
        n: usize,
        m: usize,
        a: [[usize;n];m]
    }
    let mut tup:Vec<(usize, usize)> = Vec::with_capacity(0);
    let mut flag:Vec<bool> = Vec::with_capacity(0);
    for i in 0..n{
        for j in i+1..n{
            tup.push((i+1, j+1));
            flag.push(false);
        }
    }
}*/

